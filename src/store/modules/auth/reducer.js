/** Essa funcao retorna o estado alterado de acordo a alteração da action */
// estado inicial
const INITIAL_STATE = {

};

export default function auth (state = INITIAL_STATE, action ){
    switch(action.type) {
        default:
            return state;
    }
}
